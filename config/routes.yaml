main:
  path: /
  controller: App\Controller\Main::saved

authentication:
    resource: "routes/authentication.yaml"

game:
    resource: "routes/game.yaml"
    prefix:   /game

duizenden:
    resource: "routes/duizenden.yaml"
    prefix:   /duizenden

lobby:
    resource: "routes/lobby.yaml"
    prefix:   /lobby
